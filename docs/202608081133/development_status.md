# UrbanGuard 개발 현황 — 2026-08-08 11:33

> 통합 도시안전 관제 솔루션 · 앤시정보기술(주)
> 기준 문서: `../ui_design_spec.md` v4 (화면 34개)
> 코드 위치: `D:\dev-PoC\UrbanGuard` — `flood_t` 는 원본 보존용이며 수정하지 않습니다.

## 이번 갱신에서 달라진 것 (08-08 04:52 → 11:33)

| # | 내용 |
|---|---|
| 1 | **인파 CCTV 선택 탐지 UI 완성** — S-30에 지점 선택 + 30초 분석 |
| 2 | **상시 탐지를 3개 도메인 전체로 확대** — 침수 전용이던 것을 인파·노면까지 |
| 3 | **인파·노면 상시 탐지 루프 신설** (`service/continuous.py`) |
| 4 | 노면 분석의 스트림 조회를 DB로 연결 (파일에서만 찾던 결함) |
| 5 | 관측 스냅샷에 개인정보 마스킹 적용 |
| 6 | `Thread._stop` 이름 충돌 수정 (3개 클래스) |
| 7 | 테스트 273 → **285건** |
| 8 | **신규 문서 7종 작성** (아키텍처·인터페이스·보안·개인정보·시험·매뉴얼·설치) |

---

## 1. 한눈에 보기

| 항목 | 수치 |
|---|---|
| 설계 화면 | 34개 |
| **구현 완료** | **26개** (신규 18 + 기존 자산 이관 8) |
| 일부 구현 | 2개 |
| 미구현 | 6개 |
| 자동 테스트 | **285개 통과** |
| DB 테이블 | **12개** (+ alembic 관리 1) |
| 마이그레이션 | **7개** |
| 소스 규모 | 98개 파일 · 14,384줄 |
| HTTP 경로 | 76개 |

> 설계서에 없던 **비밀번호 변경 화면**을 추가로 만들었습니다(계정을 발급받아도
> 바꿀 방법이 없어 실사용이 불가능했습니다). 위 34개에는 포함하지 않았습니다.

**구현 순서 ①~⑪ 완료, ⑫~⑮ 미착수.** ⑭(외부 연계)는 규격 미확보로 **착수 불가** 상태입니다.

### 가장 중요한 성과 네 가지

1. **무인증 SMS 발송 취약점 제거** — 인증 도입 전에는 누구나 주민에게 문자를 보낼 수 있었습니다
2. **탐지에서 대응까지 연결** — 탐지 → 이벤트 → 통보 → 시설물 제어 권고까지 한 흐름
3. **지자체 자체 운영 가능** — CCTV 등록·ROI 설정·임계값 조정이 모두 화면에서 됩니다
4. **3개 도메인 상시 탐지를 지점별로 켜고 끌 수 있습니다** — 부하에 맞춰 주기를 달리 설계

### 가장 큰 미해결 세 가지

1. **외부 연계 규격을 하나도 확보하지 못했습니다** — 통합플랫폼·112·119·CBS·차단시설
2. **AI 모델이 부산 실환경에서 검증되지 않았습니다** — 3종 모두. 인파는 아직 mock
3. **실제 알림 발송이 되지 않습니다** — SOLAPI 실발송 검증 미완

---

## 2. 구현 완료 (26개 화면 + 비밀번호 변경)

### 2-1. 인증·계정

| ID | 화면 | 내용 |
|---|---|---|
| S-00 | 로그인 | 실패 5회 잠금 · 계정 존재 여부 비노출 |
| — | 비밀번호 변경 | **임시 비밀번호는 변경 전까지 다른 화면 차단** |
| S-90 | 사용자·권한 관리 | 계정 발급 · 역할·담당 도메인 부여 · 비활성화 |
| S-91 | 감사 로그 | 로그인·계정변경·통보·설정변경 기록. MGR은 본인 부서만 |

**전역 인증 가드**를 미들웨어로 구현했습니다. 엔드포인트마다 붙이는 방식은 빠뜨리기 쉬워 이번 취약점의 원인이 됐으므로, **경로 규칙표로 한곳에서 판정**하고 **규칙 미등록 경로는 기본 거부**합니다.

### 2-2. 이벤트 (관제의 중심)

| ID | 화면 | 내용 |
|---|---|---|
| S-01 | 통합 상황판 | **지도**(외부 타일 없음) + 이벤트 큐 + 도메인 요약 |
| S-02 | 이벤트 목록 | 도메인 무관 · 상태/도메인 필터 |
| S-03 | 이벤트 상세·조치 | 확인 · 조치기록 · 부서/기관 통보 · 오탐 신고 · 종결 |

- 침수 파이프라인 → 이벤트 **자동 생성**(20초 주기)
- 같은 지점의 열린 이벤트는 하나만 유지, **최고 등급 별도 보존**
- 등급이 내려가도 자동으로 닫지 않음 (종결은 사람 판단)

### 2-3. 대응

| ID | 화면 | 내용 |
|---|---|---|
| S-11 | 시설물 원격제어 | **제어 모드 3단계**(권고만/원격요청/자동), 기본값 「권고만」 |
| S-50 | 알림 승인 대기함 | 2인 승인 · 본인 요청 자가승인 차단 |
| S-51 | 통보 이력 | 요청·승인·발송 기록 |

**알림 3계층**을 분리했습니다.

| 계층 | 되돌리기 | 승인 |
|---|---|---|
| 내부 통보 | 가능 | 관제요원 단독 |
| 기관 통보(112·119) | 정정 가능 | 관제요원 단독 |
| **주민 경보** | **불가** | **2인 승인** (역할 무관) |

### 2-4. 도메인

| ID | 화면 | 상태 |
|---|---|---|
| S-20~23 | 침수·교통위험 4종 | 기존 자산 이관 (제품 셸 적용) |
| S-30~33 | 인파관리 4종 | 기존 자산 이관 + **CCTV 선택 탐지 신규** |
| S-40~41 | 도로 노면 2종 | 기존 자산 이관 |
| S-43 | **현장 제보 접수** | 신규 — 사람 마스킹 · 원본 미보관 |

#### S-30 CCTV 선택 탐지 (신규)

인파 관제 화면 맨 위에 「CCTV 선택 탐지」를 붙였습니다. 지점을 골라
**30초 관측**하며, 남은 시간이 카운트다운으로 표시됩니다.

- 결과: 인원 최대·평균, 밀집지수, 위험행동(배회·침입), 관측 마지막 화면
- 위험등급은 **관측 구간의 최댓값**을 남깁니다(마지막 값이 아님)
- 스냅샷은 **사람 영역을 픽셀화**해 표시하고, 가리지 못하면 표시하지 않습니다
- 검출 소스가 mock이면 **경고 배너**를 띄웁니다
- 분석기가 하나뿐이라 동시 실행을 막습니다(409)

> **⚠️ 30초에 분석 프레임이 5장 정도로 얇습니다.** 검출 추론이 1장에 수 초
> 걸려 1초 간격 표본을 맞추지 못합니다. 관측 시간이나 간격 조정이 필요합니다.

### 2-5. 분석

| ID | 화면 | 내용 |
|---|---|---|
| S-60 | 통계·성과 리포트 | 기간별 집계 · 일자별 추이 · **CSV 내보내기** |
| S-61 | AI 모델 운영 | 모델별 오탐률 · 재학습 데이터 확보 현황 |

### 2-6. 설정

| ID | 화면 | 내용 |
|---|---|---|
| S-80 | 감시지점 관리 | 추가·수정·삭제 + 검증 |
| S-81 | ROI 설정 | **캔버스 편집기** — CLI 도구 불필요 |
| S-82 | 위험도 임계값 | YAML 편집 (주석 보존) |
| S-83 | 알림 규칙 | YAML 편집 |
| S-84 | AI 모델 설정 (2종) | YAML 편집 |
| S-85 | 기관 정보·화면 | 기관명 + 상황판 배경색 |

---

## 3. 일부 구현 (2개)

| ID | 화면 | 현재 | 남은 것 |
|---|---|---|---|
| S-22 | 사건 브리핑·보고서 | 기존 모달로 조회·출력 가능 | 대외 배포용 **확정** 절차 |
| S-31 | 위험행동 이벤트 | 실시간 표시만 | **이력 저장** — 인파 상시 구동 구조 필요 |

## 4. 미구현 (6개 화면)

| ID | 화면 | 사유 | 착수 가능 |
|---|---|---|---|
| S-04 | 교대 인수인계 | 미착수 (⑬단계) | ✅ |
| S-10 | 자원·출동 관리 | 미착수 (⑫단계) | ✅ |
| S-12 | 재난문자(CBS) 요청서 | 미착수 (⑬단계) | ✅ |
| S-42 | 도로 보수 요청·이력 | 현재 이벤트로 대체 중 | ✅ |
| S-34 | 인파 혼잡도 예측 | **운영 데이터 6개월+ 필요** | ❌ |
| S-70 | 외부 연계 상태 | **연계 규격 미확보** | ❌ |

### 화면 외 미착수

| 항목 | 사유 |
|---|---|
| 접근성 대응 | ⑮단계 · 전 화면 대상 |
| 시민 공개 제보 | 무인증 업로드 방어(용량·악용·스팸) 선행 필요 |

---

## 5. 기술 기반

### 5-1. DB (PostgreSQL 17.7)

| 테이블 | 용도 |
|---|---|
| `users` / `user_domains` | 계정·역할·담당 도메인 |
| `events` / `event_actions` | **이벤트와 조치 이력** (관제의 중심) |
| `notifications` | 통보 요청·승인·발송 |
| `facility_controls` | 시설물 제어 시도 (성공·실패 모두) |
| `citizen_reports` | 현장 제보 |
| `audit_logs` | 감사 추적 |
| `app_settings` | 화면에서 바꾸는 운영 설정 |
| **`cameras`** | **CCTV 지점 원장** (기존 `blocks.json` 대체) |
| **`camera_domains`** | **지점 × 탐지서비스 — 사용 여부와 상시 탐지 스위치** |
| **`camera_rois`** | **지점 × 탐지서비스별 관심영역** |

상세는 `db_design_spec.md`(v2)를 보십시오.

로컬 개발용 PostgreSQL은 `.tools/pgsql` 에 있습니다 — **시스템 설치가 아니라 폴더 안에만 존재**하므로 폴더를 지우면 DB도 사라집니다.

### 5-2. 코드 구성

```
src/tot_dashboard/
├── core/          제품 기반 계층 (신규 19개 모듈)
│   ├── roles.py       권한 매트릭스 — 설계서 6-2절이 곧 이 파일
│   ├── guard.py       전역 인증 가드 (기본 거부)
│   ├── events.py      이벤트 생성·중복억제·조치
│   ├── notifications.py  3계층 알림·2인 승인
│   ├── facilities.py  시설물 제어 3단계 모드
│   ├── reports.py     현장 제보 + image_mask.py 개인정보 마스킹
│   ├── analytics.py   통계 집계
│   └── config_edit.py YAML 주석 보존 편집
├── service/       라우트 13개 모듈 · 템플릿 19개
└── (flood/crowd/road/traffic_weather — 기존 탐지 도메인, 그대로)
```

### 5-3. 실행

```bash
cd D:\dev-PoC\UrbanGuard
.\run.ps1 -Background     # 기동 (PostgreSQL 자동 기동 포함)
.\stop.ps1 -Status        # 상태 확인
.\stop.ps1 -All           # 웹 + PostgreSQL 종료
```

---

## 6. 앞으로 해야 할 일

### 6-1. 착수 가능 — 우선순위 순

| # | 작업 | 비고 |
|---|---|---|
| 1 | **로깅 전환** | `print()` 15개 파일 잔존. 공공기관 감사 요건 |
| 2 | ⑫ 자원·출동 관리 (S-10) | CAD 축소판. GPS 추적은 범위 제외 |
| 3 | ⑬ 교대 인수인계 (S-04) | 3교대 대응 공백 방지 |
| 4 | ⑬ CBS 요청서 (S-12) | 직접 송출 불가 — 문안 생성까지 |
| 5 | S-42 보수 요청·이력 | 도로 도메인 완결 |
| 6 | ⑮ 접근성 대응 | 색상 단독 전달·키보드 조작·대체 텍스트 |
| 7 | 배포 자동화 | Dockerfile·CI 없음 |
| 8 | 운영 문서 | 설치·운영·장애 대응 |

### 6-2. 외부 조건이 갖춰져야 착수 가능

| 작업 | 막고 있는 것 | 확인처 |
|---|---|---|
| **S-70 외부 연계** | 통합플랫폼 연계 규격 | 국토부 / 구축 지자체 |
| **S-12 실제 CBS 연동** | CBS 연계 가능 여부 | 행안부 / 지자체 재난부서 |
| **S-11 원격 제어** | 차단시설 연동 규격 · **책임 범위** | 도로관리청 / 제조사 / 법무 |
| **실제 알림 발송** | SOLAPI 실발송 검증 | 자체 (P1-4) |
| **강수·하천 실측** | KMA·HRFCO 인증키 | 기상청 / 홍수통제소 |
| **S-34 혼잡도 예측** | 운영 데이터 6개월+ | 시범운영 필요 |
| **지도 배경** | 지자체 GIS 서버 | 지자체 GIS 담당 |
| **모델 실환경 검증** | 부산 실침수·실인파 데이터 | 시범운영 필요 |

### 6-3. 보안·운영 잔여

| 항목 | 현재 | 필요 |
|---|---|---|
| 세션 관리 | 서명 쿠키 | **강제 로그아웃·동시접속 제한 불가.** 필요 시 세션 테이블 |
| 비밀번호 이력 | 직전 값만 비교 | 「최근 N개 재사용 금지」 요구 시 이력 테이블 |
| 차량번호판 마스킹 | **미구현** | 전용 모델 또는 담당자 육안 확인 유지 |
| HTTPS | 미적용 | 운영 시 `URBANGUARD_COOKIE_SECURE=1` 필수 |
| 개인정보 영향평가 | 미확인 | 배회탐지·제보사진 대상 여부 |

---

## 7. 정직하게 남기는 한계

### 7-1. 실환경 검증이 안 된 것

- **AI 모델 3종 모두 부산 실환경 미검증**입니다. 침수 모델은 해외 데이터로 학습해 val F1 0.9392를 냈지만 부산 실침수 영상으로 확인하지 못했습니다
- **도로 노면 모델은 탐지 0건**입니다. 해외 공개데이터 3회(RDD2022·Mendeley·Zenodo)가 모두 실패했고, 현장 제보(S-43)로 부산 데이터를 쌓는 것이 현재 유일한 경로입니다
- **인파 검출기 기본값이 mock**입니다. 실검출 전환은 화면에서 가능하나 부산 CCTV 실영상 검증이 선행돼야 합니다

### 7-2. 화면은 있으나 실제로 동작하지 않는 것

| 화면 | 상태 |
|---|---|
| 시설물 제어 | 모든 시설 **「미연계」** — 명령이 기록만 되고 전달되지 않음 |
| 기관 통보(112·119) | **기록만** — 실제 전송 안 됨 |
| 알림 발송 | **발송 차단 모드** — 승인해도 문자가 나가지 않음 |
| 강수·하천 | **모의값(sine)** — 실측 API 미연동 |
| **인파 검출** | **모의(mock) 소스** — 인원·위험행동이 실제 영상값이 아님 |
| **노면 탐지** | 모델은 도나 **부산 CCTV에서 탐지 0건** (도메인 갭) |

이 상태를 화면마다 고정 문구로 표시했습니다. **동작하는 것처럼 보이는 것이 가장 위험**하기 때문입니다.

### 7-3. 실측 검증 현황

08-08 갱신에서 **실서버 기능 시험을 수행했습니다.** 상세는
`test_plan_report.md` 3절입니다.

| 확인한 것 | 결과 |
|---|---|
| CCTV 등록·수정·삭제, 좌표 검증 | ✅ |
| 도메인별 ROI 저장·필수 검증 | ✅ |
| 3개 도메인 상시 탐지 기동 | ✅ |
| 인파 상시 탐지의 이벤트 생성 | ✅ |
| 인파 선택 탐지 30초 실행 | ✅ (단, 소스가 mock) |
| 실 HLS 스트림 수신 | ✅ 15초에 735프레임 |
| 마스킹 모델 동작 | ✅ |

**여전히 확인하지 못한 것**

| 항목 | 사유 |
|---|---|
| **다수 지점 상시 탐지 시 CPU** | 지점 1개소로만 시험 |
| **장시간 연속 운전** | 미수행 |
| **부하·동시접속** | 미수행 |
| 기상·하천 API | 미시험 |
| 문자 실발송 | dry-run만 |
| 브라우저 호환성 | 개발 브라우저만 |

### 7-4. 상표

**솔루션명 `UrbanGuard` 는 특허청 정식 상표 조회를 거치지 않았습니다.** 웹 검색상 국내 소프트웨어 업계 충돌은 확인되지 않았으나, 대외 공표·인쇄물 사용 전 키프리스 42류 조회가 필요합니다. 그전까지는 **내부 명칭**으로만 취급해 주세요.

---

## 8. 1차 납품 범위 결정이 필요합니다

화면 34개 중 26개가 됐지만, **어디까지를 1차 납품으로 볼지 정해지지 않았습니다.**

제 판단으로는 **지금 상태가 「시범운영 가능한 최소 제품」**에 해당합니다.

- 인증·권한·감사추적 완비
- 탐지 → 이벤트 → 조치 → 통보 흐름 완결
- 지자체 자체 운영(지점·ROI·임계값) 가능

다만 시범운영에 들어가려면 **최소한 아래 셋은 필요**합니다.

1. **SOLAPI 실발송 검증** — 알림이 실제로 나가야 의미가 있습니다
2. **KMA 인증키** — 강수가 모의값이면 위험도 판정이 실제와 무관합니다
3. **배포 환경 결정** — 망분리 여부에 따라 연계 방식이 전부 달라집니다

---

## 참고 문서

### 설계·명세
- `../ui_design_spec.md` — 화면 설계서 (34개 화면 정의)
- `system_architecture.md` — **시스템 아키텍처 설계서**
- `db_design_spec.md` — **데이터베이스 설계서 v2**
- `interface_spec.md` — **인터페이스 정의서**

### 보안·개인정보
- `security_design.md` — **보안 설계서**
- `privacy_assessment.md` — **개인정보 영향평가 사전검토서**

### 시험·운영
- `test_plan_report.md` — **시험 계획·결과서**
- `install_guide.md` — **설치 가이드**
- `operator_manual.md` — **운영자 매뉴얼**

### 기획
- `../solution_productization_plan.md` — 제품화 계획
- `../crowd_behavior_detection_plan.md` — 인파 도메인 기획
- `../road_surface_management_plan.md` — 도로 도메인 검증 이력
- `../flood_water_dataset_workflow.md` — 침수 데이터 구축
- `../yolo_license_alternatives.md` — ⚠️ **AGPL 라이선스 검토 (결론 필요)**
- `docs/brand/README.md` — 브랜드 자산 사용 규칙
